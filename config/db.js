var mongoose = require('mongodb');
var localUrl = 'mongodb://localhost:27017/sentient'
/*var db = {
	mongoose.connect(localUrl, function(err,database) {
    if(err) { 
        console.error(err);
     }
    db = database; // once connected, assign the connection to the global variable
    })
};
module.exports = db;*/